import 'package:flutter/material.dart';

class DataTablesScreen extends StatelessWidget {
  const DataTablesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Data Tables'),
      ),
      body: Container(),
    );
  }
}
